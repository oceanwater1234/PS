#include<iostream>
#include<climits>
#include<string>
#include<vector>
#include<algorithm>
using namespace std;
typedef long long ll;
ll abs(ll tmp) {
    if(tmp >= 0) return tmp;
    else return -1 * tmp;
}
int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

	int t = 3;

	while (t--) {
		vector<ll> v; 
        ll n,tmp;

        cin >> n; 
    }
			
   return 0;
}